"""ticketbot.engine: the run loop and the QUESTION:/DEFER: escalation protocol.

Only `protocol.py` exists here so far (section 4) — the pipeline, selector, gates,
locks, budget and orchestrator modules land in section 8.
"""
